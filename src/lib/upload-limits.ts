/** Parkeringsskisser — brukes i klient og server (ingen Node-imports). */
export const MAX_PARKING_IMAGE_BYTES = 8 * 1024 * 1024;

/** Storyboard / shot i timeplan — samme tak som parkering. */
export const MAX_SCHEDULE_SHOT_IMAGE_BYTES = 8 * 1024 * 1024;
